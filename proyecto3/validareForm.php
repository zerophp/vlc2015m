<?php

/**
 * Validate form
 * 
 * @param array $form definition
 * @param array $data filtered
 * @return boolean | array ('fildname'=>'error message') $validatedata
 */
function validateForm($form, $datafiltered)
{
    foreach ($form)
        $valor = $datafiltered
        $validaciones = array (// segun definicion)
        foreach ($validaciones)
            switch()
                case 'required';
                case 'minsize' 
        
    return $validatedata;
}