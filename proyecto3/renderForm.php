<?php

/**
 * Render form
 * 
 * @param array $form definition
 * @param array $data postdata
 * @return string $html
 */
function renderForm($form, $data=null)
{
    foreach($form)
        switch()
            case 'text';
                $html.=''
            case 'hidden';
            case 'email';
            case 'textarea'
    return $html;
}