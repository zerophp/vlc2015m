<?php

/**
 * Filter data form
 * 
 * @param array $form definition
 * @param array $data postdata
 * @return array
 */
function filterForm($form, $data)
{
    foreach ($data)
        $valor = 
        $filtros = array('segun la definicion')
        foreach($filtros)
            switch()         
                case 'striptags'
        
    return $filterdata;    
}